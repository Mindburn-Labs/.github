apiVersion: app-mindburn-web.org/v1
kind: ObservabilityCatalog
metadata:
  name: app-mindburn-web-github-profile
spec:
  slo:
    - name: profile-metadata-valid
      target: Profile metadata must parse and mirror the canonical architecture docs.
  alerts: []
