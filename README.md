# Mindburn Labs GitHub Profile Repo

This repository owns organization profile metadata for GitHub. It is not the canonical architecture source. Use the root workspace docs for the polyrepo end state and roadmap.

Local validation:

```sh
make lint
```
